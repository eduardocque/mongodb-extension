# Codeception Test Suite Configuration
#
# Suite for unit or integration tests.

actor: UnitTester
coverage:
  enabled: true
modules:
  enabled:
    - \mongodb\Helper\Unit
    - Yii2:
        part: [init]